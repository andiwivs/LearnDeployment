
To execute build, open PowerShell Interactive Window and run:

> .\build.ps1			# run default / first task

> .\build.ps1 init2		# run specific task named "init2"

> .\build.ps1 Clean		# run Clean task